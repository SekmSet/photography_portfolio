const BASE_URL = 'http://localhost:5288/api'

const URL_AUTH =  `${BASE_URL}/Auth`
const URL_SIGN =  `${URL_AUTH}/Register`
const URL_LOGIN =  `${URL_AUTH}/Login`

const URL_USER =  `${BASE_URL}/User`

export {
  URL_AUTH,
  URL_SIGN,
  URL_LOGIN,

  URL_USER,
}
